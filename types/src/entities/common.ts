/** MongoDB ObjectId, represented as its string form over the wire. */
export type ObjectId = string;

/** ISO 8601 date-time string, as produced by JSON-serializing a Date. */
export type ISODateString = string;
